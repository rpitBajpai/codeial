<li>
    <p>
        <% if(locals.user && locals.user.id == comment.user.id){ %>
        <small>
            
            <a href="/comments/destroy/<%= comment.id%>">x</a>
        </small>
        <% } %>
        <%= comment.content%>
        <br>
        <small>
            <%= comment.user.name%>
        </small>


        <!-- CHANGE::display the Likes for this comment. If the user is logged in, then show the link to toggle likes; else, just show the likes count -->
        <br>
        <small>
            <% if(locals.user){ %>
                <a class="toggle-like-button" data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%= comment._id %>&type=Comment">
                    <%= comment.likes.length %> Likes
                </a>
                
            <% }else{ %>
                <%= comment.likes.length %> Likes
            <% } %>
        </small>



    </p>
</li>